'use strict';

const fs = require('fs'); //  操作文件或文件流
const path = require('path'); //  操作路径

// function copy(src, dst) {
//   fs.writeFileSync(dst, fs.readFileSync(src));
// }

// function main(argv) {
//   copy(argv[0], argv[1]);
// }

// main(process.argv.slice(2));
